document.addEventListener("DOMContentLoaded", function(event) {
	// Get version li element
	const version = document.querySelector('#version');
	version.innerHTML = "Version: " + CURRENT_SECTION.version
})
